# GitHub Copilot Repository Instructions

Follow the canonical rules in `AGENTS.md`. Keep domain/application layers free of adapter imports,
preserve `answered | abstained | blocked`, fail verification and hard budgets closed, use explicit
API scopes, and add boundary-appropriate tests. The full verification commands are in `AGENTS.md`.
